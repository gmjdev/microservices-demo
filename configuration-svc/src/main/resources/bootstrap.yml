spring:
  application:
    name: configuration-svc
encrypt:
  keyStore:
    location: ${KEYSTORE_PATH:classpath:cert/keystore.p12}
    password: ${KEYSTORE_PHRASE:localhost}
    alias: ${CONFIG_ENCRYPTION_KEY_ALIAS:configserverkey}
    secret: ${CONFIG_ENCRYPTION_PHRASE:localhost}