apiVersion: v1
kind: Service
metadata:
  name: greet-svc
  labels:
    app: greet-svc
spec:
  clusterIP: None
  selector:
    app: greet-svc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: greet-svc
  labels:
    app: greet-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: greet-svc
  template:
    metadata:
      labels:
        app: greet-svc
    spec:
      containers:
      - name: greet-svc
        image: registry.localhost:5000/greet-svc:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 3
        startupProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 5
          failureThreshold: 30
          periodSeconds: 10
